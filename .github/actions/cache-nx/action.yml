name: Cache Nx
description: Restore cached Nx task outputs.

runs:
  using: composite
  steps:
    - name: Cache Nx
      uses: actions/cache@v5.0.5
      with:
        path: .nx/cache
        key: ${{ runner.os }}-nx-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-nx-
