# Typia validation codegen — needed for ALL bun execution (runtime + dev server).
preload = ["./packages/lmao/src/bun/preload.ts"]

[test]
# Typia codegen + LMAO test tracing (flushes spans to .trace-results.db).
preload = ["./packages/lmao/src/bun/test-preload"]
