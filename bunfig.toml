[install]
linker = "isolated"
globalStore = true

[run]
preload = ["@smoothbricks/validation/bun/preload"]

[test]
# Register native ttsc routing before the tracing preload imports source modules.
preload = ["@smoothbricks/validation/bun/preload", "./packages/lmao/src/bun/trace-preload.ts"]
