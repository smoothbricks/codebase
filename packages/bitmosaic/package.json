{
  "name": "@smoothbricks/bitmosaic",
  "version": "0.0.0",
  "private": true,
  "nx": {
    "name": "bitmosaic",
    "targets": {
      "bench": {
        "executor": "nx:run-commands",
        "cache": false,
        "options": {
          "command": "cargo bench --locked -p bitmosaic --profile bench --bench ops --bench patch",
          "cwd": "{workspaceRoot}"
        }
      }
    }
  }
}
