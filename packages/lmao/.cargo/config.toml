[alias]
# Package-local shortcuts stay scoped even though Cargo now resolves the
# repository-root workspace.
t = "test -p 'lmao-*'"
proptest-heavy = "test -p 'lmao-*' --release -- --include-ignored --nocapture"
wasm = "build -p lmao-wasm --target wasm32-unknown-unknown --release"
