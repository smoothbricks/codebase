{
  "executors": {
    "bounded-exec": {
      "implementation": "./dist/executors/bounded-exec/executor.js",
      "schema": "./src/executors/bounded-exec/schema.json",
      "description": "Run a command with bounded timeout and force-kill behavior"
    },
    "cargo-test": {
      "implementation": "./dist/executors/cargo-test/executor.js",
      "schema": "./src/executors/cargo-test/schema.json",
      "description": "Compile Cargo test binaries, then run staged copies in parallel with per-binary timeouts"
    },
    "typescript-emit": {
      "implementation": "./dist/executors/typescript-emit/executor.js",
      "schema": "./src/executors/typescript-emit/schema.json",
      "description": "Emit transformed JavaScript with ttsc and declarations with native tsc"
    },
    "clean-outputs": {
      "implementation": "./dist/executors/clean-outputs/executor.js",
      "schema": "./src/executors/clean-outputs/schema.json",
      "description": "Remove declared build output directories unless they contain tracked files"
    },
    "napi-cross-toolchain": {
      "implementation": "./dist/executors/napi-cross-toolchain/executor.js",
      "schema": "./src/executors/napi-cross-toolchain/schema.json",
      "description": "Extract the pinned @napi-rs cross toolchain so --use-napi-cross never downloads it"
    }
  }
}
