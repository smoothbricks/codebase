{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "TypeScript emit executor",
  "description": "Emit transformed library or test artifacts with ttsc before execution",
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "enum": ["library", "javascript", "tests"],
      "default": "library",
      "description": "Library emits JavaScript and declarations; javascript honors a separate declaration target; tests emit private runner artifacts"
    },
    "executableOutputs": {
      "type": "array",
      "description": "Package bin outputs to make executable after JavaScript emit",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    },
    "cwd": {
      "type": "string",
      "description": "Working directory, relative to the workspace root"
    },
    "tsConfig": {
      "type": "string",
      "description": "TypeScript project config, relative to cwd"
    }
  },
  "required": ["cwd", "tsConfig"],
  "additionalProperties": false
}
