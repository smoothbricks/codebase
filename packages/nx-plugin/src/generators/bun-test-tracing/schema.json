{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Configure Bun test tracing",
  "description": "Configure a workspace package for the standard Bun test tracing + dist-test TypeScript setup",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Nx project name, package name, or project root to configure"
    },
    "opContextModule": {
      "type": "string",
      "description": "Module specifier that exports the package op context"
    },
    "opContextExport": {
      "type": "string",
      "description": "Named export for the package op context",
      "default": "opContext"
    },
    "spanContextModule": {
      "type": "string",
      "description": "Module specifier that exports the span context type"
    },
    "spanContextExport": {
      "type": "string",
      "description": "Named type export for the span context",
      "default": "SpanContext"
    },
    "tracerModule": {
      "type": "string",
      "description": "Module specifier for makeBunTestSuiteTracer/TestTracer",
      "default": "@smoothbricks/lmao/testing/bun"
    }
  },
  "required": ["project", "opContextModule", "spanContextModule"]
}
