diff --git a/lib/internal/projectInputPathIdentity.js b/lib/internal/projectInputPathIdentity.js
index 6f625764f4f834774bd1311373aa43f173130d36..b8c94985f096df7254a99b0d27cc9edb8075a903 100644
--- a/lib/internal/projectInputPathIdentity.js
+++ b/lib/internal/projectInputPathIdentity.js
@@ -169,6 +169,11 @@ function filesystemDirectoryIsCaseSensitive(directory, platform, operations) {
                 rejectedAlternate = true;
                 continue;
             }
+            // A sibling is only an optional case probe, not an input. Sandboxes
+            // may deny it while allowing this directory and other candidates.
+            // Keep looking for proof; never classify denial as a missing name.
+            if (isDeniedFilesystemProbe(error))
+                continue;
             throw error;
         }
     }
@@ -193,7 +198,7 @@ function filesystemDirectoryIsCaseSensitive(directory, platform, operations) {
                     return false;
                 }
                 catch (error) {
-                    if (isMissingFilesystemEntry(error) === false)
+                    if (!isMissingFilesystemEntry(error) && !isDeniedFilesystemProbe(error))
                         throw error;
                 }
             }
@@ -270,6 +275,11 @@ function isMissingFilesystemEntry(error) {
         "code" in error &&
         (error.code === "ENOENT" || error.code === "ENOTDIR"));
 }
+function isDeniedFilesystemProbe(error) {
+    return (error instanceof Error &&
+        "code" in error &&
+        (error.code === "EACCES" || error.code === "EPERM"));
+}
 function createProjectInputPathIdentityContext(operations = {}) {
     return createFilesystemPathIdentityContext(operations);
 }
